version: 0.2

phases:
  build:
    commands:
      - echo build iniciado
      - mvn clean package
  post_build:
    commands:
      - echo build completed 
artifacts:
  files:
    - target/apptestgabi.jar
  discard-paths: yes  